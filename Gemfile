source "https://rubygems.org"

# Align dependencies with GitHub Pages build environment.
gem "github-pages", group: :jekyll_plugins

# Needed for local `bundle exec jekyll serve` on modern Ruby.
gem "webrick", "~> 1.8"
